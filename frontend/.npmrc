# 解决 npm optional dependencies 跨平台问题（npm/cli#4828）
# EdgeOne 构建机是 Linux x64，本地 Windows 生成的 lock 文件不包含 Linux 的 native binding
# 删除 lock 文件 + 让 npm 安装时正确解析当前平台的 optional deps
registry=https://registry.npmjs.org/
optional=true
